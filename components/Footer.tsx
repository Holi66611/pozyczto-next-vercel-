export default function Footer(){
  return (
    <div className="container" style={{padding:'10px 0 30px'}}>
      <div className="notice"><b>Demo:</b> uruchom jednorazowo <code>/api/dev/init</code> i <code>/api/dev/seed</code>.
      <div><small>Potem: /login → /add → /search.</small></div></div>
    </div>
  );
}
